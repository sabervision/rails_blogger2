<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
    <h4><%= link_to tag.name, tag_path(tag) %></h4>
    
    <%= link_to "-> destroy", tag_path(tag), :method => :delete %>
    </li>
  <% end %>
</ul>